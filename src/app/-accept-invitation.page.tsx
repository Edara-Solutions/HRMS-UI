export function AcceptInvitationPage() {
  return (
    <section className="mx-auto flex min-h-dvh max-w-xl flex-col justify-center px-4 sm:px-6">
      <p className="text-sm font-medium text-[var(--color-primary)]">Invitation</p>
      <h1 className="mt-3 text-3xl font-semibold">Accept invitation</h1>
      <p className="mt-3 text-sm leading-6 text-[var(--color-text-muted)]">
        Stub route for `POST /auth/accept-invitation` with token, new password, and `clientType:
        "web"` from `company-portal-api.md`.
      </p>
    </section>
  );
}
